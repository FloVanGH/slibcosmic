import { CosmicWindow, StandardButton, PrimaryButton, TextButton, Toggle, Body, Settings } from "_imports/cosmic.slint";

export component Cosmic inherits CosmicWindow {
    min-width: 600px;
    min-height: 300px;
    title: "COSMIC Design System - Slint";

    VerticalLayout {
        padding: 16px;
        spacing: 12px;
        alignment: start;

        HorizontalLayout {
            alignment: start;
            spacing: 4px;

            PrimaryButton {
                text: "Default";
            }

            PrimaryButton {
                text: "Disabled";
                enabled: false;
            }
        }


        HorizontalLayout {
            alignment: start;
            spacing: 4px;

            PrimaryButton {
                text: "Default";
                destructive: true;
            }
            PrimaryButton {
                text: "Disabled";
                destructive: true;
                enabled: false;
            }
        }

        HorizontalLayout {
            alignment: start;
            spacing: 4px;

            StandardButton {
                text: "Default";
            }
            StandardButton {
                text: "Disabled";
                enabled: false;
            }
        }

        HorizontalLayout {
            alignment: start;
            spacing: 4px;

            TextButton {
                text: "Default";

                clicked => {
                    Settings.dark-color-scheme = !Settings.dark-color-scheme;
                }
            }
            TextButton {
                text: "Disabled";
                enabled: false;
            }
        }

        HorizontalLayout {
            alignment: start;
            spacing: 4px;

            TextButton {
                text: "Default";
                destructive: true;
            }
            TextButton {
                text: "Disabled";
                destructive: true;
            }
        }

        HorizontalLayout {
            alignment: start;
            spacing: 4px;

            Toggle {}

            Toggle {
                enabled: false;
            }
        }

        HorizontalLayout {
            alignment: start;
            spacing: 8px;

            Toggle {
                toggled(checked) => {
                    Settings.dark-color-scheme = !checked;
                }
            }

            Body {
                text: "Light";
                vertical-alignment: center;
            }
        }
    }
}
