import { Typography, Brushes } from "../styling/styling.slint";

export component CosmicWindow inherits Window {
    default-font-size: Typography.body.font-size;
    default-font-weight: Typography.body.font-weight;
    background: Brushes.bg-color;
}